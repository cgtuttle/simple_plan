<h2>Edit Account</h2>

<%= form_for(@account) do |f| %>
	<div class="field">
		<%= f.label :name, "Name", :class => "field_label" %><br />
		<%= f.text_field :name %><br />
	</div>
	<div class="actions">
		<%= f.submit "Update" %>
	</div>
<% end %>