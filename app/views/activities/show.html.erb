<%= render :partial => "shared/show", :locals => { :object => @activity } %>
	
	<div>
		<p><%= link_to "Edit", edit_activity_path(@activity) %></p>
		<p><%= link_to "Delete", @activity, :method => :delete, :confirm => "Please confirm deletion:",
																						:title => "Delete #{@activity.name}" %></P>
		<p><%= link_to "Back", :back %></p>
	</div>																		