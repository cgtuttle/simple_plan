<%= form_for(@activity) do |f| %>
	<div class="field">
		<%= f.label :name, "Add New Activity", :class => "field_label" %><br />
		<%= f.text_field :name %>
	</div>
	<div class="actions">
		<%= f.submit "Submit" %>
	</div>
<% end %>