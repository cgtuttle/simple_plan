<h2>Edit Role</h2>

<%= form_for(@role) do |f| %>
	<div class="field">
		<%= f.label :name, "Edit Role", :class => "field_label" %><br />
		<%= f.text_field :name %>
	</div>
	<div class="actions">
		<%= f.submit "Update" %>
	</div>
<% end %>